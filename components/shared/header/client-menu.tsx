// prostore\components\shared\header\client-menu.tsx

'use client';

import Menu from './menu'; // menu uses hooks, so this stays client

const ClientMenu = () => {
  return <Menu />;
}

export default ClientMenu;
